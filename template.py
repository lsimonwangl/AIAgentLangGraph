"""
Vector RAG - 偏好搜尋模板
=======================
template.py 負責將使用者問題改寫成適合搜尋過往旅遊偏好的查詢文字。

執行流程：
    0. 接收使用者原始旅遊問題
    1. 將問題包裝成偏好檢索語境
    2. 指定檢索器搜尋景點類型、住宿評價、預算分配等個人化資訊
    3. 回傳改寫後的查詢文字給 preferences.py 的 profile 節點使用

此模組提供 build_preference_query() 函式供 preferences.py 呼叫。
"""


def build_preference_query(user_query: str) -> str:
    """回傳一段搜尋用文字，讓檢索器知道要找哪些旅行偏好資訊。"""
    # 將使用者原始問題包裝成偏好檢索查詢
    return (
        f"根據使用者目前的旅遊需求「{user_query}」，"
        "從之前旅行紀錄中檢索與本次行程風格相關的內容，"
        "找出使用者的旅行風格偏好，包括景點類型、住宿評價、預算分配等個人化資訊，"
        "以及下次想去的地方。"
    )
